<?xml version="1.0" encoding="UTF-8"?>

<!-- 마이바티스 3 Mapper DTD -->
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
	<mapper namespace="web.dao.face.FileUploadDao">
	
	<insert id="SourceInsertSource" parameterType="web.dto.Source">
		<selectKey keyProperty="sourceNo" order="BEFORE" resultType="int">
			select source_seq.nextval from dual
		</selectKey>
	insert into source (source_no, source_name, bpm, key, source_img_originname, tag_no, pack_no, source_img_storedname)
	values( #{sourceNo} , #{sourceName} , #{bpm}, #{key} , #{sourceImgOriginname} , #{tagNo} , #{packNo} , #{sourceImgStoredname) ) 
	
	</insert>
   
   
  	
  	</mapper>